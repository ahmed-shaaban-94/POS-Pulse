# POS v3.5 Convergence — Defect Register

Running log of defects found while converging the terminal onto the v3.5 prototype.
Protocol: spec §6. **Internal** defects are fixed test-first within their slice; **contract**
defects (anything that changes what crosses the wire to Data-Pulse-2) are escalated to the owner
and STOP at the architecture boundary — never fixed unilaterally.

| id | slice | screen / area | description | evidence (file:line) | severity | class (internal\|contract) | status (open\|fixed-with-test\|escalated) |
|----|-------|---------------|-------------|----------------------|----------|----------------------------|-------------------------------------------|
| D-001 | Phase 0 | integration / route-guard test | Flaky test: `cashier-route-enumeration.test.tsx` non-deterministic (~11/15 fail). **ROOT CAUSE (systematic debug 2026-06-21):** NOT URL/isolation bleed (first hypothesis, falsified). The async guard redirect mounts `route-sign-in` and unmounts `app-shell` on separate React commits; the tests asserted the second element's *absence* SYNCHRONOUSLY right after `await waitFor()` on the first element's *presence*, so they observed the transient window where both co-exist. **FIX:** fold each presence+absence pair into a single `waitFor` (condition-based waiting) so RTL polls until both states settle. Verified 0 failures / 20 consecutive runs (was 11/15). Pre-existing test bug (PR #398), surfaced as a CI merge-gate flake. | `tests/integration/renderer/cashier-route-enumeration.test.tsx` | medium | internal | **FIXED 2026-06-21** (test-only; condition-based waitFor; 20/20 green) |
| D-002 | Phase 0 | tooling / eslint config | `npm run lint` reports ~7426 errors, all `.js` parse errors from the untracked in-tree GitHub Actions runner (`actions-runner/bin/*.js`). `eslint.config.js` `ignores` block omits `actions-runner/**`, so ESLint's project service tries to parse vendored runner JS. Pre-existing (our 2 commits touched 0 `.js` / nothing under actions-runner). CSS is not linted by eslint. Lint gate is red on baseline, independent of convergence. | `eslint.config.js:8` (ignores block) | medium | internal | open (deferred — eslint scope fix; do NOT delete runner per infra rule) |
| D-005 | Slice 2 | catalogue / sale-pane honesty surfaces | **CONTRACT GAP (not an engine bug).** Prototype `SaleScreen` honesty surfaces need data the catalogue contract does NOT carry. `ProductSnapshotDisplay` (the real search/scan result) has: name_ar/_en, price_minor, unit_pack_label, tax_category, selling_barcode, sku, active, controlled_substance, prescription_required. It has **NO** stock level, batch expiry, drug-interaction pairs, frequently-bought-together, or per-unit split price. So interaction-warnings / expiry-chips / stock-levels / bought-together are NOT buildable now (need DP-2 POS-stock contract, POS-013). Also: `prescription_required`/`controlled_substance` are "surfaced for awareness only, enforcement out of scope" — UI may show an Rx/controlled BADGE but NOT a blocking add-gate (that would assert enforcement the contract disclaims). `tax_category` not threaded (VAT = tax-pending, ADR-0003). | `src/shared/catalogue/product-snapshot.ts` (ProductSnapshotDisplay); `bridge-api.ts:1164-1267` (catalogue.*) | medium | **contract** | **ESCALATED / deferred-as-shell. Owner decision 2026-06-21:** Rx = display-only badge (NON-blocking, no ref/dosage capture — honors "enforcement out of scope"); honesty surfaces (stock/expiry/interaction/bought-together) = enrichment-runway shells ("data not available yet"), fill in when POS-013 DP-2 contract lands + ratified. Slice 2 builds: search/scan/results/3 errors/freshness/add-to-cart + Arabic display + pack label + Rx/controlled badge + shell slots. |
| D-004 | Slice 1 | shell / nav integration test | Coverage gap (not a code defect): `navigation.test.tsx` `renderApp` helper wires only 5 routes (dashboard/sales/cart/inventory/settings); the new `/app/returns` + `/app/audit` nav entries' click→route-renders path is not exercised in the integration harness. Their link attributes ARE covered at contract level (`NavRail.test.tsx`). | `src/renderer/routes/app/__tests__/navigation.test.tsx:44-54` | low | internal | open (deferred — add returns/audit to renderApp in a later slice or coverage pass) |
| D-003 | Slice 1 | open-shift / operator engine | **CONTRACT GAP — open-shift screen cannot be wired (roadmap-confirmed G2).** Prototype has a full `OpenShiftScreen` (opening float → records openedAt/openedBy → routes to sale). Renderer engine has **no open/start-shift path**: `operator-session-store` models only sign-in/out/takeover; operator bridge has `forceCloseShift`/`listStuckShifts`/`dismissShiftClosedNotice` but **no `openShift`/`startShift`**. **Verified on DP-2 `origin/main` (2026-06-21):** `pos-shifts` module exists but exposes ONLY `GET /api/pos/v1/shifts/stuck` — **no `POST` open-shift operation.** Orchestrator roadmap places open-shift in Phase 4 as UI-shell-only, persistence **deferred & STOP at G2** (POS-015 blocked on DP-2 shift contract). | `bridge-api.ts:360-509`; `operator-session-store.ts:28-46`; DP-2 `pos-shifts.openapi.yaml` (stuck-only); orchestrator roadmap Phase 4 / repo-spec-matrix:137 | high | **contract** | escalated — G2-deferred & roadmap-tracked: open-shift removed from Slice 1 → Slice 12 (UI-shell only); sign-in + shell proceed on the live operator engine. |
| D-006 | Slice 1 | route gating + RTL isolation (CodeRabbit #434) | **3 real bugs from external review.** (1) P1: `/app/audit` + `/app/returns` routes were under the no-`allow` parent guard → a cashier could reach surfaces the role-visibility-matrix marks ⛔ (audit) / Phase-7-blocked (returns). (2) P2: NavRail was role-blind → cashiers saw Audit/Returns links. (3) P2: shell `dir="rtl"` bidi-reordered money/SKU (`SearchResultRow` price/code, `LineItemRow` mono) and mirrored the PinPad keypad — handoff requires Latin/mono LTR isolation. | `router.tsx:183`; `shell-routes.ts`; `NavRail.tsx`; `SearchResultRow.tsx:53/60`; `LineItemRow.tsx:89/92`; `PinPad.tsx:105` | high | internal | **FIXED 2026-06-21 (test-first):** routes gated to manager/admin + NavRail role-filtered (cashier 5, mgr/admin 7); `dir="ltr"` on keypad grid + price/code/money spans. Full suite 5443 green. 2 CodeRabbit nits skipped w/ in-thread reasons (vendored CLAUDE.md v3.0 label per roadmap; kit.css keyframes — no stylelint in repo + vendored file). |
| D-008 | Slice 3 | cart line — product flags not threaded (Codex #436 P2) | The display-only LineItemRow props (prescriptionRequired/controlledSubstance/packLabel) are never populated on real cart lines, so Rx/controlled badges + pack label don't appear post-add. ROOT: the cart line snapshot `CartLineItem` + add-path `AddedLineResult` carry only displayName/unitPriceMinor/lineSubtotalMinor/note/version — product flags are NOT in the cart contract. Not a recompose bug; threading them = engine/contract change (deferred). | `CartPane.tsx:50` (CartLineItem); `:62` (AddedLineResult) | low | **contract** | deferred — props are display-ready for when the cart line carries flags; wiring needs a CartLineItem/add-path shape change (engine), owner-gated. Replied in-thread on #436. |
| D-007 | Slice 3 | cart pane — VAT + hold-sale (contract limits) | Two prototype cart features have NO engine path (verified audit): (1) the cart engine computes ONLY `subtotal` (Σ lineSubtotalMinor, integer minor units) — **no VAT field, no grand-total**; (2) **no hold/suspend/park** cart action exists. So the prototype's subtotal/VAT/total footer + F3 hold-sale cannot be fully wired. | `cart-store.ts` (subtotal-only FSM); `bridge-api.ts cart.*` (no hold/suspend) | medium | internal | **RESOLVED in Slice 3 (workflow-verified):** VAT renders as a tax-pending PLACEHOLDER (`—` / `tax-pending`, NOT a computed 14% — ADR-0003); Total = Subtotal until tax lands; hold-sale rendered as a permanently-DISABLED enrichment shell (no bridge call); drug-interaction warning = enrichment shell. Money/audit reviewer APPROVED: engine/FSM/attribution-gate byte-identical, no VAT arithmetic, no new audit emit. |

## Primitive API baseline (Task 2)

Frozen prop surface of each reused primitive, captured 2026-06-21. Convergence binds to these as-is;
any prototype need beyond this is logged in the table above (internal = additive prop; contract = engine data).

| primitive | file | prop surface (frozen) |
|---|---|---|
| Button | `ui/primitives/Button/Button.tsx` | `intent, size?, children, iconStart?, iconEnd?, disabled?, loading?, type?, onClick?` (+AriaAttributes) |
| Input | `ui/primitives/Input/Input.tsx` | `variant, label, description?, errorMessage?, disabled?, value?, defaultValue?, onChange?` |
| Badge | `ui/primitives/Badge/Badge.tsx` | `intent (info\|success\|warning\|danger\|neutral), children` (+Aria) |
| Card | `ui/primitives/Card/Card.tsx` | `variant?, children, aria-labelledby?` (+Aria) |
| Dialog | `ui/primitives/Dialog/Dialog.tsx` | `open, onOpenChange, variant?, title, description?, children, primaryAction?, secondaryAction?` |
| Table | `ui/primitives/Table/Table.tsx` | `rows, columns, state?, emptyMessage?, errorMessage?` (generic `<Row>`) |
| StatusBanner | `ui/primitives/StatusBanner/StatusBanner.tsx` | `state (ConnectionState), message?` |
| Toast | `ui/primitives/Toast/Toast.tsx` | `intent, title, description?, durationMs?, onDismiss?` |
| PinPad | `ui/operator/PinPad.tsx` | `value, onChange, onSubmit, disabled?` |
| OperatorBadge | `ui/operator/OperatorBadge.tsx` | `display_name, role` |
| AmountPad | `ui/payments/AmountPad.tsx` | `valueMinor, onChange, totalMinor` |
| MoneyRoll | `ui/payments/MoneyRoll.tsx` | `valueMinor, className?` |

**Parity verdict (Task 2):** All primitives are prop-sufficient for the prototype. **No prop-API change
and no contract escalation required.**
- Badge intents match prototype exactly (`info/success/warning/danger/neutral`).
- StatusBanner `ConnectionState` (online/degraded/offline/syncing) covers all prototype banner states.
- Dialog supports the Rx-gate pattern (children + primary/secondary actions; pos-app.jsx:604-633).
- **AmountPad note (internal, no change):** prototype uses free-entry keypad; production `AmountPad`
  takes `totalMinor` for quick-amount suggestions but does not constrain entry, so it expresses the
  prototype's voucher-remainder / insurance-copay / credit-down-payment flows via the existing prop
  surface. Adaptation is at the call site, not the primitive. No D-row needed.
